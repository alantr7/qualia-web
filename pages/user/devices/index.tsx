import {NextApiRequest, NextApiResponse} from "next";
// import withUser from "../../../lib/with-user";
/*
const handler = async (req, res) => {
    await res.json({ a: 'Hello!' });
};*/
/*
export default withUser(handler);*/

export default () => {
    return (
        <div>

        </div>
    )
};